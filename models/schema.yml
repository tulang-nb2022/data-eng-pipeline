version: 2

sources:
  - name: weather_data
    description: "Weather data stored in S3 (silver)"
    tables:
      - name: weather_partitioned
        description: "Date-partitioned weather data"
        meta:
          external_location: "s3://data-eng-bucket-345/silver/weather/"